# ⚠️  DEPRECATED — This workflow has been split into two focused workflows:
#
#   dr-select-repos.yml  →  Manually select which ADO repos to mirror (updates repos.json)
#   dr-mirror.yml        →  Daily scheduled mirror based on repos.json
#
# This file is kept only for reference and is NOT active (no 'on:' trigger).
# You can safely delete this file once you have verified the new workflows work correctly.
